import React from 'react';
import Login from '../components/Login';

/**
 * in case using state must be overwritten into a react class
 * */
function LoginContainer() {
  return (
    <Login />
  );
}

export default LoginContainer;
